# CLAUDE.md — <!-- TODO: ATOM name -->

<!--
  Minimal fixture standing in for thecore's real samples/ATOM_CLAUDE.md -- exercises the
  thecore:atom generator's fetch mechanism offline (thecore_generators#22); not a copy of the
  real file, which this gem doesn't own and shouldn't duplicate.
-->

## Gem overview

<!-- TODO: what this ATOM is for. -->

## Claude Code skills

Matt Pocock's skill set is installed as the `mattpocock-skills` Claude Code plugin, on each
developer's own **host** machine.

## Development workflow

### Required skill sequence for new features and codebase changes

**Before any new implementation request or brainstorming session**, `/ask-matt` should run first.

## Common commands

```bash
bundle exec rake test
bundle exec rubocop
```

## External integration

<!-- TODO: any external integration this ATOM owns. -->
