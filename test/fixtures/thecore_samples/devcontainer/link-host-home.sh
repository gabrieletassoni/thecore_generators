#!/usr/bin/env bash
# Minimal fixture standing in for thecore's real samples/devcontainer/link-host-home.sh
# -- only needs to exist and be marked executable after the template chmods it; the App
# template's test doesn't execute it.
exit 0
