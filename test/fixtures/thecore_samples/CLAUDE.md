# CLAUDE.md — <!-- TODO: project/app name -->

<!--
  Minimal fixture standing in for thecore's real samples/CLAUDE.md -- exercises the App
  template's fetch mechanism offline (thecore_generators#18); not a copy of the real
  file, which this gem doesn't own and shouldn't duplicate.
-->

## Repository structure

<!-- TODO: this app's own directory layout. -->

## Claude Code skills

Matt Pocock's skill set is installed as the `mattpocock-skills` Claude Code plugin, on each
developer's own **host** machine.

## Development workflow

### Required skill sequence for new features and codebase changes

**Before any new implementation request or brainstorming session**, `/ask-matt` should run first.

## Common commands

```bash
bundle exec rails server
bundle exec rails test
bundle exec rubocop
```

## External integration

<!-- TODO: this app's own external integrations, if any. -->
